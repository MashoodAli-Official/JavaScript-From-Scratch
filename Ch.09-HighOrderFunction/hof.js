// HIGH ORDER FUNCTION in javascript
// In javascript, a function which is taking one or more other functions as an argument or returns a function as a result is called a higher-order function as a argument is called High order function.

const num1 = [2,4,6,8,10];
const doubleNum = num1.map((num) => num * 2);
console.log(doubleNum)